<nav class="navbar">
  <div class="row" style="border-bottom:solid 1px black; padding: 10px 20px">
    <div class="col-xs-2">
      <h3>LOGO</h3>
    </div>

    <% if admin_signed_in? %>
      <div class="col-xs-10 text-right">
        <%= link_to admins_products_path do %>
          <button type="button" class="btn btn-default navbar-btn">
            <span style="color:blue">商品一覧</span>
          </button>
        <% end %>
        <%= link_to admins_users_path do %>
          <button type="button" class="btn btn-default navbar-btn">
            <span style="color:blue">会員一覧</span>
          </button>
        <% end %>
        <%= link_to admins_orders_path do %>
          <button type="button" class="btn btn-default navbar-btn">
            <span style="color:blue">注文履歴一覧</span>
          </button>
        <% end %>
        <%= link_to admins_categories_path do %>
          <button type="button" class="btn btn-default navbar-btn">
            <span style="color:blue">ジャンル一覧</span>
          </button>
        <% end %>
        <%= link_to destroy_admin_session_path, method: :delete do %>
          <button type="button" class="btn btn-default navbar-btn">
            <span style="color:blue">ログアウト</span>
          </button>
        <% end %>
      </div>
      <div align="right">
        <%= form_tag(search_path, :method => "get" ) do %>
          <div class="input-group">
            <%= text_field_tag :search,params[:search], placeholder: "search" %>
            <%= button_tag(type: "submit") do %>
              <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
            <% end %>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="col-xs-10 text-right">
        <%= link_to admins_products_path do %>
          <button type="button" class="btn btn-default">
            <span style="color:blue">商品一覧</span>
          </button>
        <% end %>
        <%= link_to new_admin_session_path do %>
          <button type="button" class="btn btn-default">
            <span style="color:blue">ログイン</span>
          </button>

        <% end %>
      </div>
    <% end %>
  </div>
</nav>
<style>
  .btn {
    margin: 10px 10px;
  }
</style>